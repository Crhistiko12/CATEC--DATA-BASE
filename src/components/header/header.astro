---
import Nav from "../nav.astro";
---

<header class="bg-[rgb(251,176,59)] text-white sticky top-0 z-50 shadow-md">
  <div class="container mx-auto px-4 py-3 flex justify-between items-center">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-6 md:gap-4">
      <img
        src="/img/logo/LOGO-FINAL.svg"
        alt="KukiMascotas"
        class="h-16 md:h-20 lg:h- w-auto"
        width="120"
        height="120"
      />
      <span class="font-bold text-xl md:text-2xl hidden sm:inline"
        >KukiMascotas</span
      >
    </a>

    <!-- Navegación -->
    <Nav />

    <!-- Carrito -->
    <a href="/carrito" class="relative p-2">
      <span class="sr-only">Carrito</span>
      🛒
      <span
        class="absolute -top-1 -right-1 bg-black text-white text-xs rounded-full h-5 w-5 flex items-center justify-center"
        >0</span
      >
    </a>
  </div>
</header>
